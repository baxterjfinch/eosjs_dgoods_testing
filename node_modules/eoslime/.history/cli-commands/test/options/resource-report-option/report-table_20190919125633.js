const CLITable = require('cli-table');

const TABLE_HEAD = {
    head: ['Contract', 'Action', 'Transaction ID', 'NET', 'CPU', 'RAM']
}

class ReportTable {
    constructor() {
        this.table = new CLITable(TABLE_HEAD);
    }

    addRow(rowData) {
        rowData[4] = `${rowData[3]}`;
        rowData[3] = `${rowData[3]} Bytes`;
        rowData[5] = `${rowData[3]} Bytes`;
        this.table.push(rowData);
    }

    draw() {
        console.log(this.table.toString());
    }
}

module.exports = new ReportTable();
