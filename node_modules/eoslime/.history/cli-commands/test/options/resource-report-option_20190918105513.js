const Option = require('./../../option');

class ResourceReportOption extends Option {
    constructor() {
        super(
            'network',
            {
                "describe": "Provides you a report for with all contract functions used in the the tests and their RAM and Bandwidth usage",
                "type": "network name or in case of custom { url: custom url, chainId: custom chain id }",
                "default": "local",
                "choices": Provider.availableNetworks().all
            }
        );
    }

    process(network) { }
}

module.exports = new NetworkOption();
