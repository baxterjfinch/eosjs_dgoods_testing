const ContractFactory = require('./../../src/contract/contract-factory');

class ContractFactoryStub extends ContractFactory {

    constructor(contractName) {
        this.contractName = contractName;
    }

    async at(contractName, contractExecutorAccount = this.provider.defaultAccount) {
        super.at(this.contractName);
    }
}

module.exports = ContractFactoryStub;
