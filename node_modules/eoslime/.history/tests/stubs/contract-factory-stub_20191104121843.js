const ContractFactory = require('./../../src/contract/contract-factory');

let contractName = '';

class ContractFactoryStub extends ContractFactory {

    constructor(provider) {
        super(provider);
    }

    async at(contract, contractExecutorAccount = this.provider.defaultAccount) {
        return super.at(contractName, contractExecutorAccount);
    }

    static setContract(contract) {
        contractName = contract;
    }
}

module.exports = ContractFactoryStub;
