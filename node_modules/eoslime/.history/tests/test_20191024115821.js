const proxyHandler = {
    get: (obj, value) => {
        console.log(value)
    }
}

const a = new Proxy({}, proxyHandler);

const b = {
    [Function]: () => { return 5 },
    brum: 10
}

console.log(b())