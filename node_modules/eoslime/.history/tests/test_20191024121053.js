class ProxyHandler {
    bam() {
        console.log('You are very clever')
    }
}

const proxyHandler = {
    // get: (obj, value) => {
    //     console.log('here')
    // }
}

const a = new Proxy((value) => {
    console.log(value)
}, new ProxyHandler());

const a = new Proxy(new ProxyHandler(), {});

console.log(a.bam)