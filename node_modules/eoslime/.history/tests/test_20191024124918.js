class ProxyHandler {
    bam() {
        console.log('You are very clever')
    }
}
// const b = new ProxyHandler();

const proxyHandler = {
    get: (obj, value) => {
        // if (obj.hasOwnProperty(value)) {
        console.log(obj[value])
        return obj[value]
        // if (typeof value == 'function') {
        //     console.log(here)
        // }
        // }
    }
}

// const a = new Proxy((value) => {
//     console.log(value)
// }, proxyHandler);

// a(5)
// a.bam()

const a = new Proxy(new ProxyHandler(), proxyHandler);
a.bam()
