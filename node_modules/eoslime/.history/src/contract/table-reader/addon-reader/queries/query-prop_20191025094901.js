class QueryProp {
    let props = { default: true };
    public queries: Array < QueryProp > =[];

    public push(queries: Array<QueryProp>) {
    for(let i = 0; i<queries.length; i++) {
    this.queries.push(queries[i]);
}
    }
}

export default QueryProp

