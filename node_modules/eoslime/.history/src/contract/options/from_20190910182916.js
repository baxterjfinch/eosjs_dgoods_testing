const Account = require("./../../account/account");

function fromOption(options, context) {
    let authorizationAccount;
    if (optionals && optionals.from instanceof Account) {
        return optionals.from;
    }

    return context.executor;
}
