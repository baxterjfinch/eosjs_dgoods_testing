module.exports = function (data) {
    return {
        instanceOf: function (ofObject) {
            if (data == null || data == undefined) {
                throw new Error(`Not an instance of ${ofObject}`);
            }

            console.log(Object.getPrototypeOf(Object.getPrototypeOf(Object.getPrototypeOf(data))))
            if (!(data.constructor.name === ofObject)) {
                throw new Error(`Provided ${data.constructor.name} is not an instance of ${ofObject}`);
            }
        }
    }
}

const recursivelyCheckIfInstance = function (candidate, requiredInstance) {

}