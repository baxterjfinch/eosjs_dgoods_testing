const fs = require('fs');

module.exports = {
    readABIFromFile: function (abiPath) {
        return JSON.parse(fs.readFileSync(abiPath));
    },
    readWASMFromFile: function (wasmPath) {
        return fs.readFileSync(wasmPath);
    },
    doesAbiExists: function (abi) {
        return abi === path.basename(abi)
    }
}
